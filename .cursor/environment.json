{
  "name": "Card Genie API",
  "install": "npm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Runs the Express API (http://localhost:8787) and the Vite dev server (http://localhost:5173) together via concurrently. AI card generation requires the OPENAI_API_KEY environment secret; other flows work without it."
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 8787 }
  ]
}
